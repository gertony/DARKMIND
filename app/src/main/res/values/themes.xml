<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.DarkMind" parent="Theme.Material3.DayNight.NoActionBar"/>

    <style name="MySpinnerStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Estilo para botones negros con texto blanco y esquinas rectas, ahora con borde blanco -->
    <style name="BlackRectangularButton" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">0dp</item>
        <item name="strokeColor">@color/white</item>
        <item name="strokeWidth">1dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:elevation">0dp</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:fontFamily">@font/montserrat_medium</item>
    </style>

    <!-- Nuevo estilo para botones blancos con texto negro y esquinas rectas -->
    <style name="WhiteRectangularButton" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="cornerRadius">0dp</item>
        <item name="strokeColor">@color/gray</item> <!-- Borde gris sutil para el botón blanco -->
        <item name="strokeWidth">1dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:elevation">0dp</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:fontFamily">@font/montserrat_medium</item>
    </style>

</resources>
